apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sdnr-simulator.fullname" . }}-test-connection"
  labels:
    {{- include "sdnr-simulator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sdnr-simulator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
