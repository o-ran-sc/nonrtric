apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dmaap-mr.fullname" . }}-test-connection"
  labels:
    {{- include "dmaap-mr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dmaap-mr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
