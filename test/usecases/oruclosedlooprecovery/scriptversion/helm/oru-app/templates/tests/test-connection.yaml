apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oru-app.fullname" . }}-test-connection"
  labels:
    {{- include "oru-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oru-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
